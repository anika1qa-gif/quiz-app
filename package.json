{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Simple Quiz App (HTML/CSS/JS) with tests and Jenkinsfile",
  "scripts": {
    "start": "http-server ./src -p 8080 -o",
    "test": "jest",
    "lint": "eslint .",
    "build": "node build.js"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "http-server": "^14.1.1",
    "jest": "^29.7.0"
  }
}